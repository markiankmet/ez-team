<UserControl x:Class="FullApp.View.Level_1_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FullApp.View" xmlns:viewmodel="clr-namespace:FullApp.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:HomeViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="590" d:DesignWidth="1000">
    <Grid>
        <Button
                                 Tag="{StaticResource color3}"
                                 
                                 Command="{Binding Level1Command}">
            <StackPanel Orientation="Horizontal">
                <!--<fa:IconImage Icon="ChalkboardTeacher" Style="{StaticResource menuButtonIcon}"/>-->
                <TextBlock Text="Test1" Style="{StaticResource menuButtonText}"/>
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
